# Normalize line endings: LF in the repo for all text (keeps Lua/PY/YML consistent for Linux CI).
* text=auto eol=lf

# Explicit binaries (never normalized) — belt-and-suspenders with .gitignore.
*.pak  binary
*.ucas binary
*.utoc binary
*.pdb  binary
*.zip  binary
*.png  binary
